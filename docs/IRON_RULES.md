# 闲鱼项目铁律

> ⚠️ **最高优先级，所有操作必须遵守，如有违反立即停止！**

---

## 铁律一：工作目录限制

**所有文件创建、脚本创建、代码修改等一切操作，必须在项目目录下进行：**

```
/Users/lucifer/.openclaw/workspace-xiaoyu/project-xianyu/
```

❌ **禁止** 在 `workspace/project-xianyu`、`~/.agents/skills` 等其他任何目录创建与闲鱼项目相关的文件。

---

## 铁律二：Git 提交要求

**任何变更（代码修改、脚本创建、配置更新、文档修改等），必须立即提交：**

```bash
git add .
git commit -m "描述"
git push
```

❌ **禁止** 存在未提交的变更过夜。
❌ **禁止** 批量提交多个不相关的变更。

---

## 违反后果

违反以上铁律，可能导致：
- 项目版本历史不完整
- 团队协作混乱
- 实验流程无法回溯

---

*本文件由 AI 小宇 自动生成并遵守*
